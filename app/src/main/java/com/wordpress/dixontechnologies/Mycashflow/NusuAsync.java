/*
package com.wordpress.dixontechnologies.Mycashflow;

import android.content.Context;
import android.os.AsyncTask;

public class NusuAsync extends AsyncTask<Void, Void, Void> {

    // Context context_;
  private  NusuViewModel _nusuViewModel;
  private  CashItems cashItems_;


    private NusuAsync(CashItems cashItems, NusuViewModel nusuViewModel) {
        cashItems_ = cashItems;
        _nusuViewModel = nusuViewModel;
    }


    @Override
    protected Void doInBackground(Void... voids) {
      voids[0] =  _nusuViewModel.insertCash(cashItems_);
        return _nusuViewModel.insertCash(voids[c]);
    }
}
*/
